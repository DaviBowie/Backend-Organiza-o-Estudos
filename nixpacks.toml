[phases.setup]
nixPkgs = ["python312", "chromium", "playwright-driver"]

[phases.install]
cmds = [
  "python3.12 -m ensurepip --upgrade",
  "python3.12 -m pip install -r requirements.txt",
  "playwright install chromium"
]

[start]
cmd = "uvicorn main:app --host 0.0.0.0 --port $PORT"
